[app]
name = "RustVerse"
version = "1.4.4"
description = "Rust Toolchain Visual Version Manager"

[binaries]
rustup = "rustup"
cargo = "cargo"

[paths]
# Cargo command-line tool directory, where Cargo-related binaries are stored
cargo_bin_relative = ".cargo/bin"
# user data directory, where user data is stored
data_dir = "$EXE_DIR/data"
# log directory, where application logs are stored
log_dir = "$EXE_DIR/logs"
# Webview directory, where Webview-related data is stored
webview_dir = "$EXE_DIR/webview"
# Temp directory, where temporary files are stored
temp_dir = "$EXE_DIR/temp"

[database]
db_type = "redb"
db_name = "rustverse_db"

[locale]
codes = ["en","zh-CN"]

# Locale metadata (auto-generated, do not edit manually)
[locale.meta."en"]
  name = "English"
  english_name = "English"
[locale.meta."zh-CN"]
  name = "简体中文"
  english_name = "Chinese Simplified"






















































































































































































[timeouts]
cargo_search_seconds = 30

[events]
install_log = "install-log"
install_finished = "install-finished"
plugin_install_log = "plugin-install-log"
plugin_install_finished = "plugin-install-finished"
update_log = "update-log"
update_finished = "update-finished"

[plugins.official]
names = ["cargo-clippy", "cargo-fmt", "cargo-miri", "cargo-rustdoc", "cargo-test-fixture", "rustfmt", "clippy", "miri"]

[parsing]
default_marker = "(default)"
active_marker = "(active)"
installed_marker = "(installed)"
no_overrides = "no overrides"
up_to_date = "Up to date"
update_available = "Update available"
version_separator = " -> "
status_separator = " - "
cargo_prefix = "cargo-"
