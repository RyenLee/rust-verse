//! Infrastructure configuration — AppConfig model, defaults, and DB assembly.
//!
//! Re-exports all public types from sub-modules for backward compatibility.

pub mod app_config;
pub mod defaults;

pub use app_config::*;